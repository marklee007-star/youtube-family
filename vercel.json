{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/expire?token=${CRON_SECRET}",
      "schedule": "1 0 * * *"
    }
  ]
}
